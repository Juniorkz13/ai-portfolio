class RetrievalService:
    def retrieve(self, question: str, top_k: int = 5) -> list[dict[str, str | int]]:
        # Placeholder: compute question embedding and perform pgvector similarity search.
        _ = (question, top_k)
        return []
